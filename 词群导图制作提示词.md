# 词群导图制作提示词（SOP）

> 用户说「做词群导图」「给某 unit 加词群导图」时，严格按本提示词执行。
> 本提示词固化了 cet6_quiz.html 词群导图功能的全部规格（数据、UI 元素位置、交互、浏览器集成、约束），目标是让任何新 unit 的导图与当前 unit1 的表现完全一致。

---

## 一、整体流程（每个新 unit）

1. **收素材**：用户提供书页「词以群记」导图截图（每词群一张）。
2. **OCR 识别**：用 vision skill（`node ~/.claude/skills/vision/scripts/vision.js`）识别每张截图——词、音标、释义、词群层级（父节点/子节点）、关系标签（扩/同根/近义/相关/形近/加前缀/反义/拆分/合成）。
3. **生成数据**：写入/追加 `data/unit-maps.js`（格式见第二节）。
4. **代码接入**：词群导图渲染/定位代码已在 `cet6_quiz.html` 中实现（buildMapSVG / renderGroupMap / groupMapDrill / groupMapSpeak / findMapGroup / groupMapSVG 等），**新 unit 无需改这些代码**，只需保证数据就位。若数据已覆盖新 unit，`canUseNewMap` 自动生效（见第五节）。
5. **浏览器集成**：导图自动出现在单词浏览器的「🗺️ 导图」按钮下（见第七节），无需额外改动。
6. **覆盖校验**：新 unit 的**全部词**必须在导图数据中能找到（零缺失），词根/主题词也要作为一级节点进数据。
7. **测试**：按第九节补测试，全量跑过（`npx playwright test`）。
8. **提交**：用 Git 环境变量身份提交（见第十节）。

---

## 二、数据规格（data/unit-maps.js）

- **文件格式**：`window.__UNIT_MAPS_DATA__ = {...};`，**LF 行尾**。文件头可有说明注释。
- **数据结构**（`unit → gid → 词群`）：
  ```js
  {
    "1": {
      "1": {
        "group_name": "雄心相关",
        "level1Nodes": [
          {
            "word": "ambition",
            "phonetic": "/æm'bɪʃn/",
            "meaning": "n.野心;雄心,抱负",
            "pos": "n",
            "relation": "扩",            // 可省略（一级节点常无 relation）
            "children": [ { ...同样结构... } ]  // 无子节点时 children: []
          }
        ]
      }
    }
  }
  ```
- **字段顺序**固定：`word, phonetic, meaning, pos, relation, children`。
- **覆盖要求**：
  - 该 unit 核心词库（`cet6_quiz.html` 内联 `id="data-all-words"` 的 ALL_WORDS）里该 unit 的**每一个词**都必须出现在导图数据某个词群节点中（含二级/更深层）。
  - 词根/主题词（如 rise、gene、precede）若未出现在书页节点里，也要作为该词群的一级节点补入（relation 按同词群风格），保证做题词一定可见。
  - **音标/释义/词性一律以词库权威数据为准**（从 `data-all-words` 对应词取），绝不自行编造。词库未收录的词才保留 OCR 识别值。
- **relation 标签**取值：`扩 / 同根 / 近义 / 相关 / 形近 / 加前缀 / 反义 / 拆分 / 合成`，按书页标注；书页未标时参照同词群同类节点风格，或省略。
- **数据文件行尾**：LF（`\n`），不要 CRLF。

---

## 三、UI 规格（buildMapSVG，每个元素的位置与尺寸）

所有数值为 SVG 内部坐标（viewBox 480×380，渲染宽度 max 400px）。

- **视口/圆心**：`viewBox="0 0 480 380"`，圆心 `cx=240, cy=190`。
- **中心圆**：
  - 半径 `centerR` 动态：
    - 主题中心（中心是词群名，非单词）：`max(36, ceil(最长断行行宽/2)+9)`，字号 13，中文短语断为至多两行（`gmapCenterLines`）。
    - 单词中心（下钻后中心是单词）：单行，字号从 15 递减、**不低于 13**；`r = max(30, ceil(词宽/2)+10)`（词宽按 `字符数×0.55×字号` 估算）。
  - 填充 `var(--accent)`，描边 `var(--accent2)`，stroke-width 2。
  - 中心文字：`fill:#ffffff`，font-weight 700，`text-anchor="middle"`，`dominant-baseline="middle"`，多行用 tspan、行距 `lh`（单词 lh=fs+3，主题 lh=18）。
- **环带节奏（关键，保证不贴圆）**：
  - 中心圆边 → 关系标签中心 = **18px**（`relD = centerR + 18`）。
  - 关系标签中心 → 节点圆近边 = **18px**（对称）。
  - 节点环半径 `R = centerR + 51`（节点圆近边在 `centerR+36`）。
- **节点圆**：`r = 15`（当前词 active 时 17），中心在 `(cx + R·cosθ, cy + R·sinθ)`，θ 按 `2πi/n - π/2` 均分；填充 `var(--surface2)`（active 为 `var(--accent)`），描边 `var(--accent)`，stroke-width 1.6（active 2.6）。
- **节点文字（单词）**：**完整显示、绝不截断**。水平排布、不沿径向旋转。中心在 `(cx + txR·cosθ, cy + txR·sinθ)`，其中 **`txR = R + max(38, round(词长×2.9) + 27)`**——保证任何角度、任何词长下文字包围盒都距节点圆 ≥10px。字号 10.5，active 时 700 号白字+暗描边。
- **关系标签**：`font-size 9.5`，中心在 `(cx + relD·cosθ, cy + relD·sinθ)`，y 再上移 4；`paint-order="stroke"` + `stroke:var(--surface)` 抗遮挡。文字内侧距中心圆边 ≥4px（由 relD 保证）。
- **连线**：`stroke-width 1.4`，颜色按关系类型映射：
  ```js
  GMAP_REL_COLORS = { '扩':'var(--green)', '同根':'var(--blue)', '近义':'var(--orange)',
    '相关':'var(--text2)', '形近':'var(--pink)', '加前缀':'#0891b2',
    '反义':'var(--red)', '拆分':'var(--blue)', '合成':'var(--green)' }
  ```
  虚线（`stroke-dasharray="4,3"`）：**形近、反义**。
- **展开标记**：有子节点的节点圆正上方 `y-11` 处放 `▾`（font-size 9，`var(--accent2)`）。
- **末端词**：无子节点且该层无节点时，显示「末端词，无更多关联词」提示 div，不渲染空 svg。

---

## 四、交互规格

- **点击节点圆圈** = 下钻展开（`groupMapDrill(id, word, hasChildren)`），只下钻、不发音。
- **点击节点单词文字** = 朗读发音（`groupMapSpeak(id, word)`），只发音、不下钻。
- 有子节点的词在圆圈上方有 `▾` 提示可展开；下钻后出现「← 返回上级」按钮（`groupMapBack`）。
- 悬停节点圆圈显示 tooltip：`word + 音标 + 释义`。
- 标题栏文案：`🗺️ 词群导图：{词群名}（点击圆圈展开 · 点击单词发音）`。

---

## 五、逻辑规格（定位 / 自动下钻 / 兜底）

- `canUseNewMap(word)`：`currentPool === 'core'` 且 `word.unit` 在导图数据中存在（**不检查 word.group_id**，词库分组可能与书页不一致）。
- `findMapNodePath(map, word)`：在单个词群内递归找 word，返回下钻路径（一级为 `[]`），找不到返回 `null`。
- `findMapGroup(unitMap, word)`：遍历该 unit 的所有词群，返回 `{gid, path}` 或 `null`——**以书页词群划分为准**（词库 group_id 可能错位，如 orient 词库归 g1、书页在 g14）。
- `groupMapSVG(word)`：
  1. `canUseNewMap` 为假 → 回退 `legacyGroupMapSVG`（旧版，中心=词群名、节点=相邻词，含当前词）。
  2. `findMapGroup` 找不到该词 → **回退旧版**（不显示不含当前词的新版图）。
  3. 找到 → 用 `{unit, gid, path, active: word.word}` 渲染，**自动下钻到词所在层并高亮当前词**（active 节点 r=17）。

---

## 六、词群导图在单词浏览器中的集成

- 浏览器词条（renderBrowse，基于 FULL_WORDS）头部在「查询」旁已有 **「🗺️ 导图」按钮**，点击展开 `bdmap-{word}` 容器。
- `browseGroupMapHTML(word)`：遍历 `__UNIT_MAPS_DATA__` **所有 unit**，用 `findMapGroup` 定位浏览词实际词群（**不受 currentPool 限制**），渲染新版导图，浏览词 highligh。
- `toggleBrowseMap(word)`：首次点击先 `ensureUnitMaps` 加载导图数据再渲染；词在导图数据中找不到时显示提示「📖 本书词群导图暂无此词」。
- 新增 unit 数据后浏览器导图自动可用，无需改浏览器代码。

---

## 七、代码约束（重要）

- `cet6_quiz.html` **CRLF 行尾**；`data/unit-maps.js` **LF 行尾**。
- `renderBrowse` 等旧代码里 HTML 字符串是 **`\"` 冗余转义**存储（属性引号带反斜杠、`查询` 字面），编辑这些行时用脚本精确匹配或复制文件字节，别用普通引号。
- **禁止自编内容**：单词、音标、释义、词性、助记一律以词库/书页为准；不造词、不改释义。
- 作者是「刘晓艳」（非「刘晓燕」）。

---

## 八、测试要求（cet6_quiz.spec.js）

新增 unit 数据后，在 `group map (new: unit-maps.js data)` describe 块补/维护测试，参照现有断言：
1. 数据完整性：该 unit 词群覆盖、全部核心词在导图数据中零缺失。
2. 做题可见性：该 unit 每个词 `groupMapSVG` 后，导图节点或中心必含当前词。
3. 布局安全：全图层扫描，中心文字在圆内、节点文字距节点圆 ≥6px、节点文字完整无截断。
4. 交互：点圆圈下钻、点单词发音。
5. 浏览器：unit1 词点「导图」按钮渲染且含当前词；导图外词显示「暂无」。

跑全量 `npx playwright test` 全过后才可提交。

---

## 九、提交约束

- 每次 Git 提交必须用环境变量身份，**绝不修改 git config**：
  ```
  GIT_AUTHOR_NAME=Adrian0314 GIT_AUTHOR_EMAIL=Adrian0314@users.noreply.github.com GIT_COMMITTER_NAME=Adrian0314 GIT_COMMITTER_EMAIL=Adrian0314@users.noreply.github.com
  ```
- 提交信息用中文、`feat:`/`fix:` 前缀，说明改动与验证结果。
- 是否 push 由用户决定，默认只提交不 push。

---

## 十、完成标准

- [ ] 数据文件覆盖新 unit 全部核心词（零缺失），字段格式正确，LF 行尾。
- [ ] 数据生成后不需要改 cet6_quiz.html 渲染/定位代码（仅当新增了数据未覆盖的结构才扩展）。
- [ ] 全量测试通过（含新增/维护的断言）。
- [ ] 已提交，工作区干净。
